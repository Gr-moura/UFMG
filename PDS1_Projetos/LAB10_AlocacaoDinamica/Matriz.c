#include <stdio.h>
#include <stdlib.h>

int main()
{
    int linhas, colunas;
    scanf("%d %d", &linhas, &colunas);

    for (int i = 0; i < linhas; i++)
    {

        for (int j = 0; j < colunas; j++)
        {
        }
    }

    return 0;
}
